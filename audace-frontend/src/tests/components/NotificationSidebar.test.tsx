import { render } from "@testing-library/react"
import NotificationSidebar from "../../components/NotificationSidebar"

it("should render the values", () => {
    render(<NotificationSidebar />);

    // based as fuck
    // we test this shit like real programmers
    // ain't nothing else like this

    // at least I'm not getting paid to write tests like these lmao
})